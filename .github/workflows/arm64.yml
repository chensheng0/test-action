on: [push]

jobs:
  testactions_job:
    runs-on: ubuntu-latest
    name: A job to test the multi architecture action
    steps:
    - name: Run some commands on arm64
      id: runcmd
      uses: chensheng0/test-action
      with:
        architecture: aarch64
        distribution: ubuntu18.04
        run: |
          uname -a
